# CAP-01 判定结果

**本件不修改 `PREREG_CAP_01.md`。** 该文件自冻结至今零字节改动，其 SHA-256

```
36cc44c7394e2b23a810a5bc0d4d62bec290259935392317a0fb37458ca9b029
```

与带外时间戳所记逐位相同（`github.com/yunshen0126/prereg-timestamps`，
commit `f46a42e`，GitHub 服务端观测到推送于 2026-08-03T08:25:30Z）。
本臂开跑于 2026-08-03 16:25:41，即推送之后 11.9 秒。

`PREREG_CAP_01` §9 第 5 步要求将结果追加至该文件 §10。**该步骤不予执行**：
追加任何一个字节都会使上述逐位一致断裂，而这一致性是"判据自冻结后零改动"
的唯一直接证据。以本件履行其实质。偏离已记于 ledger §8k (e)。

**本臂不是结果盲的（`PREREG_CAP_01` §0）。** 判据在见到本臂任何数据之前冻结，
但起草于 MAIN-01 全部结果已知之后。论文中每一处报告本臂结果的位置**必须同时
标注**其动机为事后的。

---

## 1. 执行

| 项 | 值 |
|---|---|
| 设计矩阵 | 2 数据集 × 13 路径条件 × 1 负载档（`R_high`）× 4 seed = **104 run** |
| 完成 | **104 / 104，零失败，无矩阵外产物** |
| 操纵量 | `d_model` 256 → **512**（四通道各宽 128） |
| 其余配置 | 与 MAIN-01 逐字相同，SHA 门控有效 |
| 总墙钟 | 71.2 h（三进程并行） |
| `git_commit` | `68fa05d`，全部 104 个 run 一致 |
| `git_dirty` | `True`，全部 104 个 run 一致（见 ledger §8k (b)） |

"零失败"指全部 104 个 run 完成 100 epoch 并通过 `completed.json` +
`final_checkpoint.pt` + metadata SHA-256 三重校验。它**不**表示全部 run 都学到了
东西——见 §4。

判定由 `cap01_judge.py` 执行，该脚本按文件路径 import `analyze_main624.py`，
复用其 `_record_from_metadata` / `_summary` / `_format_display` /
`_completion_status`，不重新实现统计口径；其 `--selftest` 在 `outputs_main`
（`d_model = 256`）上重现论文 Table 6 的 24 项，逐位一致。

---

## 2. C0（前置）：本臂在饱和条件下是否仍有测量能力

判据（`PREREG_CAP_01` §4.0，见数据前冻结）：结构对比 ① 的 95% 区间**排除零**，
**且**十三条件的验证跨度 **> 1.0 pp**。

| | CIFAR-10 | OrganAMNIST |
|---|---|---|
| 结构组尾窗 train 中位数 | 99.92%（天花板标记） | 100.00%（天花板标记） |
| 十三条件 val 跨度 | 56.18 pp | 24.78 pp |
| 跨度 vs 1.0 pp 阈值 | 通过 | 通过 |
| 结构对比 ① | `+29.68 [-2.00, +61.37]` | `+15.80 [-29.55, +61.15]` |
| ① 是否排除零 | **否，跨零** | **否，跨零** |
| **C0** | **不通过** | **不通过** |

**两个数据集的 C0 均不通过。** 依 §4.0 表格第二行，本臂在两个数据集上均判为
**测量能力不足**；C1/C2/C3 的结果**如实报告但不予解释**，且**不得用于回应
容量意见**。

跨度远超阈值而 ① 仍跨零，是因为 ① 的区间宽达 ±31 pp（CIFAR-10）与 ±45 pp
（OrganAMNIST）。作为对照，MAIN-01 在同两个数据集的同一格上，① 分别为
`+9.94 [+9.51, +10.37]` 与 `+1.88 [+1.42, +2.34]`。

---

## 3. C1 / C2 / C3：如实报告，不予解释

**以下数字按 `PREREG_CAP_01` §4.0 的要求记录在案。由于 C0 在两个数据集上均
不通过，它们不构成任何证据，不得被解释，不得用于回应容量意见，不得写入论文
的任何主张句。**

| 量 | CIFAR-10 | OrganAMNIST |
|---|---|---|
| `P_G` | `+4.78 [+3.78, +5.78]` | `+0.26 [-0.34, +0.86]` |
| `P_R` | `-12.64 [-26.26, +0.97]` | `+6.57 [-51.26, +64.39]` |
| ② `= P_G - P_R` | `+17.42 [+4.54, +30.31]` | `-6.30 [-64.65, +52.04]` |

（`P_G` 与 `P_R` 与 ② 同时列出，依 `MAIN_PREREG_ADDENDUM_03` §3。）

- **C1**（`P_R` 是否跨容量含零）：两个数据集的 `P_R` 区间均含零。
- **C2**（CIFAR-10 的 ② 下界是否 > 0）：下界 `+4.54 > 0`。
- **C3**（OrganAMNIST 的 ② 是否含零）：含零。

### 三条事前固定的结论表述均不适用

`PREREG_CAP_01` §4.1–§4.3 为 C1/C2/C3 的各种结果预先固定了结论措辞。
**那些措辞以 C0 通过为前提（§4.0 表格第一行："C1/C2/C3 的判定有效"）。
C0 不通过时它们全部无效。**

特别地，**不得**写"分支异质性成分为零这一结果在 2 倍容量范围内稳定"，
也**不得**写"几何专属多路径增益在 2 倍容量下存活"。

`cap01_judge.py` 在 C0 不通过时仍打印了这些措辞（每行挂 `[C0 未通过, 不予解释]`
标记），是脚本的设计疏忽；该输出不得照抄。此点记于此以防误用。

---

## 4. 装置为何失去测量能力：逐格描述

**本节是描述，不是解释。** 成因的任何说法都是事后的，只能出现在论文的
Discussion（`MAIN_PREREG_ADDENDUM_03` §2）。

104 个 run 中有 **15 个未能学到任何东西**——训练准确率与验证准确率同时停在
常数输出水平：

| 数据集 | 几何族 | 任意族 | 辅助族 |
|---|---|---|---|
| CIFAR-10 | 0 / 20 | **11 / 24** | 0 / 8 |
| OrganAMNIST | 0 / 20 | 3 / 24 | 1 / 8 |

CIFAR-10 的十一个格中，train 与 val 均为 `10.00`（十类，常数输出）；
`RND_D1 seed0` 为 `15.47 / 15.28`。OrganAMNIST 的四个格为 `5.66 / 4.95`
（三个）与 `17.00 / 15.26`（一个），十一类。

这些 run 通过了完成性校验（跑满 100 epoch、marker 与 checkpoint 齐全、
metadata SHA 一致），故计入"零失败"。它们不是崩溃退出，是训练未收敛。

**未塌的部分：**

- 几何族五个条件在两个数据集上全部正常，CIFAR-10 上 val **全部优于** d256
  （`+1.15` 到 `+2.09` pp），OrganAMNIST 上 `+0.18` 到 `+0.45` pp。
- CIFAR-10 任意族未塌的 13 个 run 落在 62 pp 附近，**低于** d256 的 62.78–63.14。
- 辅助族 `LOC_S` / `LOC_D` 在 CIFAR-10 上与 d256 基本持平（`-0.37` / `-0.17`）。

**MAIN-01（`d_model = 256`）无此现象。** 该臂中两个数据集的十三个条件的四 seed
均值全部处于健康水平（CIFAR-10 任意族 62.78–63.14，OrganAMNIST 94.75–95.26）；
若四个 seed 中有一个停在常数输出，条件均值会被拉到 50 pp 上下，实测无一如此。

**不予处置。** 未对任何 run 或 seed 作排除，未调整任何阈值，未更换 `d_model`
重跑（`PREREG_CAP_01` §5 第 5 条、§6；`MAIN_PREREG_ADDENDUM_03` §9）。
预注册未规定"未收敛 run"的处置方式，事后补设即属违规。C0 是预注册为此设的
唯一装置，它已经生效。

### 一条不得被写成发现的观察

崩塌只出现在任意族与辅助族，未出现在几何族的任何一个 run。**这不构成任何
主张。** 它是 n = 4 seed、单一容量点、无重复的观察，且与崩塌本身完全混淆——
崩塌的 run 正是使对比失去意义的那些 run。它至多可作为 Discussion 中一句标明
为事后的观察，**不得进摘要、结论或任何主张句**，也不得表述为"随机扫描顺序
导致训练不稳"。若要检验该现象，须另立预注册。

---

## 5. 本臂确立了什么、没确立什么

**没确立：** 本臂**未能回应**"`P_R ≈ 0` 可能只是模型太小"这一可预见的审稿意见。
该意见在本研究中**仍然开放**。

**确立：** 在 `d_model = 512`、其余配置与 MAIN-01 逐字相同的条件下，本装置未能
保持测量能力，判定依据是一条在见到数据前冻结、并由第三方时间戳固定的门控。

**不受影响：** MAIN-01 的 624 个 run、五张表、Figure 1、以及命题 A 与 M3 的判定
结论，在本臂执行前后逐字不变（`PREREG_CAP_01` §0）。本臂不参与其投票，
不判命题 A，不判 M3（§4.5）。

**Limitations 的相应条款不变，且应被加强而非放松：** 论文的结论仍限于
`d_model = 256`。本臂的存在不构成向更大容量外推的任何依据。

`PREREG_CAP_01` §5 第 6 条：本臂**不回应**"分辨率过小"这一意见，
全部 run 仍为 32×32 输入。

---

## 6. 产物

| 文件 | 位置 |
|---|---|
| 104 个 run | `/root/autodl-tmp/outputs_cap512/` |
| 台账 | `/root/autodl-tmp/outputs_cap512/cap01_runs.tsv` |
| 判定报告 | `cap01_judge_report.txt` |
| 收尾取证 | `cap01_finalize_report.txt` |
| 逐格诊断 | `cap01_diagnose.txt` |
| 判定脚本 | `cap01_judge.py`（含 `--selftest`） |

---

**本件 SHA-256：** `<落盘后计算并回填>`
**记录于：** `docs/03_EVIDENCE_LEDGER.md` §8k
